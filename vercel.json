{
  "framework": null,
  "buildCommand": "flutter build web --release",
  "installCommand": "curl -fsSL https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_3.24.4-stable.tar.xz -o flutter.tar.xz && tar -xf flutter.tar.xz && export PATH=\"$PWD/flutter/bin:$PATH\" && flutter doctor && flutter pub get",
  "outputDirectory": "build/web",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
